type User {
  id: ID! @unique
  name: String!
  email: String!
}

type Game {
  id: ID! @unique
  players: Int!
  city: String!
  street: String!
  date: String!
  hour: String!
  description: String!
  # user: User!
}
